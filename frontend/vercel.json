{
  "buildCommand": "npm run build:deps && npm run indexer:migrate && npm run build",
  "crons": [
    {
      "path": "/api/indexer/sync",
      "schedule": "0 * * * *"
    }
  ],
  "installCommand": "npm --prefix .. ci --audit=false",
  "$schema": "https://openapi.vercel.sh/vercel.json"
}
