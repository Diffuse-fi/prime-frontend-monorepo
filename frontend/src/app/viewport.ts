import { Viewport } from "next";

const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  colorScheme: "light dark",
  viewportFit: "cover",
  userScalable: false,
  themeColor: "#18132d",
};

export default viewport;
